#ifndef FIRE_COMPILE_H
#define FIRE_COMPILE_H

//higher level functions that run Fire code.

#include "Tokenizer.h"
#include "Exec.h"

//runs and compiles an entire string of code
void Compile_compile(char* code);

#endif
